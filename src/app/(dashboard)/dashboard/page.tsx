export const dynamic = "force-dynamic";

export default function DashboardPage() {
  return (
    <div>
      <h2 style={{ fontSize: "1.25rem", fontWeight: 600, marginBottom: "1rem" }}>
        Dashboard
      </h2>
      <p style={{ color: "var(--color-muted)" }}>
        No data yet — Phase 1 coming soon.
      </p>
    </div>
  );
}
